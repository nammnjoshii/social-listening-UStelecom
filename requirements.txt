# ── Core ──────────────────────────────────────────────────────────────────────
anthropic>=0.40.0          # Claude API
pydantic>=2.6.0            # Data models & validation
python-dotenv>=1.0.0       # Load .env file

# ── Data ──────────────────────────────────────────────────────────────────────
pandas>=2.2.0              # Aggregation & trend computation
numpy>=1.26.0              # Numeric ops (required by pandas)

# ── HTTP (used for Reddit free JSON API, Instagram, Twitter) ──────────────────
requests>=2.31.0

# ── NLP / text processing ─────────────────────────────────────────────────────
langdetect>=1.0.9          # English language detection
datasketch>=1.6.4          # MinHash LSH for near-duplicate detection

# ── YouTube collection (credential-free) ──────────────────────────────────────
youtube-comment-downloader>=0.1.68    # Comments via YouTube Innertube API (no quota)
yt-dlp>=2024.1.1                      # Video discovery via yt-dlp (replaces youtube-search-python, Python 3.14 compatible)

# ── Instagram collection (credential-free) ─────────────────────────────────────
instaloader>=4.10                     # Scrapes public Instagram hashtags without API credentials

# ── App Store review collection (credential-free) ─────────────────────────────
google-play-scraper>=1.2.4            # Google Play reviews
app-store-scraper>=0.3.5             # Apple App Store reviews

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.32.0
plotly>=5.20.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity>=8.2.3            # Retry logic with exponential backoff
rich>=13.7.0               # Pretty CLI output for pipeline runner

# ── Removed (no longer needed) ────────────────────────────────────────────────
# psycopg2-binary  → replaced by SQLite (built into Python)
# sqlalchemy       → replaced by SQLite (built into Python)
# praw             → replaced by Reddit's free public JSON API (no credentials)
# tweepy           → replaced by direct requests to Twitter API v2 (optional)
# altair           → redundant with plotly
